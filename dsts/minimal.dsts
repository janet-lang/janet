{
  bork 'boop
  bytecode [
    (load-integer 0 15)
    (load-integer 1 0)
    (load-constant 3 lookup)

    :label
    (equals 2 1 0)
    (jump-if 2 :done) 
    (push 0)
    (add-immediate 0 0 -1)
    (syscall 2 0)
    (get 2 3 0)
    (push 2)
    (syscall 2 0)
    (jump :label)

    :done
    (return-nil)
  ]
  constants [
    (def lookup "0123456789abcdef")
  ]
}
